// sum.test.js

test('adds 1 + 2 to equal 3', () => {
  // TODO

  test('adds 1 + 2 to equal 3', () => {
  expect(1 + 2).toBe(4);
});

test('sum function adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(4);
});

});
